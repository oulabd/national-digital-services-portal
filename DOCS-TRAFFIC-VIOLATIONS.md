# وثيقة وحدة المخالفات المرورية
## Traffic Violations Module - Complete Implementation Guide

---

## 📋 جدول المحتويات | Table of Contents

1. [نظرة عامة](#نظرة-عامة)
2. [هيكل الملفات](#هيكل-الملفات)
3. [مميزات الوحدة](#مميزات-الوحدة)
4. [دليل الاستخدام](#دليل-الاستخدام)
5. [مرجع الدوال](#مرجع-الدوال)
6. [فئات CSS](#فئات-css)
7. [البيانات والحالات](#البيانات-والحالات)
8. [الاستجابة والطباعة](#الاستجابة-والطباعة)

---

## نظرة عامة

وحدة المخالفات المرورية هي نظام شامل لإدارة المخالفات المرورية من قبل المواطنين على بوابة وزارة الداخلية. توفر الوحدة واجهة احترافية وسهلة الاستخدام لعرض المخالفات وتتبع السداد واستخراج الوثائق.

### الأهداف الرئيسية:
- عرض شامل لجميع المخالفات المرورية
- تتبع حالة السداد بسهولة
- استخراج وطباعة الوثائق الرسمية
- معالجة الدفع عبر بوابة آمنة
- واجهة مستجيبة وسهلة الاستخدام

### المتطلبات التقنية:
- متصفح حديث يدعم HTML5 و CSS3
- JavaScript مفعل
- حجم شاشة: من 480px إلى 1920px
- ذاكرة محلية (localStorage) لتخزين البيانات

---

## هيكل الملفات

```
سوريانا/
├── traffic-violations.html          # لوحة تحكم المخالفات (الرئيسية)
├── violation-details.html           # صفحة تفاصيل المخالفة (قابلة للطباعة)
├── css/
│   ├── style.css                   # الأنماط الأساسية (موجود)
│   └── traffic.css                 # أنماط وحدة المخالفات (جديد)
├── js/
│   ├── main.js                     # الدوال الأساسية (موجود)
│   └── traffic.js                  # منطق وحدة المخالفات (جديد)
└── assets/
    ├── images/               # الصور والرموز
    └── fonts/               # الخطوط (Cairo, Tajawal)
```

### تفاصيل الملفات:

#### 1. **traffic-violations.html** (الملف الرئيسي)
- **الحجم:** ~450 سطر
- **الغرض:** لوحة تحكم المخالفات بأربعة أقسام
- **المكونات:**
  - رأس الصفحة (Header) مع معلومات المستخدم
  - الشريط الجانبي (Sidebar) للتنقل
  - نظام التبويبات (Tabs)
  - محتوى كل قسم
  - نموذج الدفع (Modal)

#### 2. **violation-details.html** (صفحة التفاصيل)
- **الحجم:** ~350 سطر
- **الغرض:** عرض تفاصيل المخالفة بصيغة رسمية
- **الميزات:**
  - تصميم وثيقة حكومية رسمية
  - قابلة للطباعة (A4)
  - قابلة للتصدير كـ PDF
  - خاتم رسمي وتوقيعات

#### 3. **css/traffic.css** (أنماط الوحدة)
- **الحجم:** ~1200 سطر
- **المحتوى:**
  - متغيرات CSS للألوان والمسافات
  - أنماط التبويبات
  - أنماط البطاقات والجداول
  - أنماط النماذج والمدخلات
  - أنماط الطباعة (Print Media)
  - استجابة الشاشات المختلفة

#### 4. **js/traffic.js** (منطق الوحدة)
- **الحجم:** ~400 سطر
- **المحتوى:**
  - إدارة الحالة (State Management)
  - تحويل التبويبات
  - تحديد المخالفات
  - معالجة الدفع
  - تصدير البيانات
  - تحديث البيانات الحية

---

## مميزات الوحدة

### 1️⃣ **مخالفاتي (My Violations)**
- عرض ملخص إحصائي:
  - إجمالي المخالفات
  - المخالفات غير المدفوعة
  - المخالفات المدفوعة
  - إجمالي المبلغ المستحق
- عرض أحدث المخالفات
- روابط سريعة لعرض التفاصيل

### 2️⃣ **سجل المخالفات (Violation Record)**
- فلاتر بحث متقدمة:
  - رقم السيارة
  - نطاق التاريخ
  - نوع المخالفة
  - حالة السداد
- جدول شامل مع:
  - معلومات المخالفة الكاملة
  - شارات حالة ملونة
  - أزرار إجراءات
- إمكانية التصدير كـ CSV

### 3️⃣ **استخراج الأوراق (Document Extraction)**
- 6 أنواع وثائق:
  1. بيان المخالفات المرورية
  2. إخطار رسمي
  3. شهادة عدم وجود مخالفات
  4. السجل الكامل
  5. بيان المبالغ المستحقة
  6. شهادة السياقة الآمنة
- أزرار الاستخراج والطباعة المرتبطة

### 4️⃣ **الدفع والمتابعة (Payment & Tracking)**
- ملخص الدفع مع:
  - عدد المخالفات المستحقة
  - إجمالي المبلغ
  - تاريخ الاستحقاق
- قائمة طرق الدفع:
  - بطاقات ائتمان
  - التحويل البنكي
  - محافظ رقمية
- قائمة المخالفات المستحقة
- نموذج دفع آمن

---

## دليل الاستخدام

### للمستخدم النهائي:

#### 1. **عرض المخالفات**
```
1. افتح صفحة traffic-violations.html
2. اختر التبويب "مخالفاتي"
3. شاهد ملخص المخالفات
4. اضغط على "عرض جميع المخالفات" للتفاصيل
```

#### 2. **البحث والتصفية**
```
1. انتقل إلى تبويب "سجل المخالفات"
2. أدخل معايير البحث (رقم السيارة، التاريخ، الحالة)
3. اضغط "تطبيق الفلاتر"
4. يمكنك حفظ النتائج كـ CSV
```

#### 3. **الحصول على الوثائق**
```
1. انتقل إلى تبويب "استخراج الأوراق"
2. اختر نوع الوثيقة المطلوبة
3. اضغط "استخراج" لتحميل أو "طباعة" للطباعة
```

#### 4. **سداد المخالفات**
```
1. انتقل إلى تبويب "الدفع والمتابعة"
2. اختر طريقة الدفع
3. اضغط "دفع الآن" للمخالفة المطلوبة
4. أدخل بيانات الدفع
5. أكمل عملية الدفع
```

---

## مرجع الدوال

### دوال إدارة التبويبات

#### `switchTab(tabId)`
تحويل التبويب النشط

**المعاملات:**
- `tabId` (string): معرّف التبويب
  - `"myViolations"` - مخالفاتي
  - `"violationRecord"` - سجل المخالفات
  - `"documents"` - استخراج الأوراق
  - `"payment"` - الدفع والمتابعة

**الاستخدام:**
```html
<button class="tab-btn active" onclick="switchTab('myViolations')">
  مخالفاتي
</button>
```

**الوظائف الداخلية:**
- إخفاء جميع التبويبات
- عرض التبويب المحدد
- إضافة فئة active للزر
- تحديث حالة التطبيق

---

### دوال عرض التفاصيل

#### `viewViolationDetails(violationId)`
فتح صفحة تفاصيل المخالفة

**المعاملات:**
- `violationId` (string): معرّف المخالفة (مثل "VIO-2024-00156")

**الاستخدام:**
```javascript
viewViolationDetails('VIO-2024-00156');
```

**السلوك:**
- ينقل المستخدم إلى صفحة violation-details.html
- يمرر معرّف المخالفة كمعامل URL

---

### دوال الطباعة

#### `printViolation(violationId)`
فتح صفحة المخالفة للطباعة

**المعاملات:**
- `violationId` (string): معرّف المخالفة

**الاستخدام:**
```html
<button onclick="printViolation('VIO-2024-00156')">طباعة</button>
```

**السلوك:**
- يفتح صفحة تفاصيل المخالفة
- يضبط النمط للطباعة
- يخفي العناصر غير المطلوبة (أزرار، رؤوس)

---

### دوال الوثائق

#### `generateDocument(docType, isPrint)`
استخراج أو طباعة وثيقة

**المعاملات:**
- `docType` (string): نوع الوثيقة
  - `"violations-statement"` - بيان المخالفات
  - `"notice"` - إخطار رسمي
  - `"no-violations-cert"` - شهادة عدم وجود
  - `"full-record"` - السجل الكامل
  - `"amounts-due"` - المبالغ المستحقة
  - `"safe-driving-cert"` - شهادة السياقة

- `isPrint` (boolean): true للطباعة، false للاستخراج

**الاستخدام:**
```javascript
// استخراج الوثيقة
generateDocument('violations-statement', false);

// طباعة الوثيقة
generateDocument('violations-statement', true);
```

---

### دوال الدفع

#### `openPaymentModal(violationId, amount)`
فتح نموذج الدفع

**المعاملات:**
- `violationId` (string): معرّف المخالفة
- `amount` (number): المبلغ المستحق

**الاستخدام:**
```javascript
openPaymentModal('VIO-2024-00156', 500);
```

**السلوك:**
- عرض النموذج
- ملء معلومات المخالفة
- تخزين المخالفة الحالية

---

#### `closePaymentModal()`
إغلاق نموذج الدفع

**الاستخدام:**
```javascript
closePaymentModal();
```

---

#### `simulatePayment()`
معالجة الدفع (محاكاة)

**الاستخدام:**
```html
<button onclick="simulatePayment()">تأكيد الدفع</button>
```

**الخطوات:**
1. التحقق من صحة البيانات
2. عرض رسالة انتظار
3. تحديث حالة المخالفة
4. إظهار رسالة نجاح
5. إغلاق النموذج
6. تحديث البيانات

---

### دوال الفلترة

#### `applyFilters()`
تطبيق فلاتر البحث

**الاستخدام:**
```html
<button onclick="applyFilters()">تطبيق الفلاتر</button>
```

**الفلاتر المدعومة:**
- `vehicleNumber` - رقم السيارة
- `dateFrom` - من التاريخ
- `dateTo` - إلى التاريخ
- `violationType` - نوع المخالفة
- `status` - حالة السداد (paid/unpaid)

---

#### `clearFilters()`
إعادة تعيين جميع الفلاتر

**الاستخدام:**
```html
<button onclick="clearFilters()">مسح الفلاتر</button>
```

---

### دوال التصدير

#### `exportTableAsCSV()`
تصدير جدول المخالفات كـ CSV

**الاستخدام:**
```html
<button onclick="exportTableAsCSV()">تصدير</button>
```

**الملف الناتج:**
- اسم الملف: `violations-record.csv`
- الترميز: UTF-8
- الفاصل: فاصلة عربية

---

### دوال تحديث البيانات

#### `updateSummaryCards()`
تحديث بطاقات الملخص

**الاستخدام:**
```javascript
updateSummaryCards();
```

**تحديثات:**
- إجمالي المخالفات
- المخالفات غير المدفوعة
- المخالفات المدفوعة
- إجمالي المبلغ المستحق

---

#### `refreshViolationTable()`
تحديث جدول المخالفات

**الاستخدام:**
```javascript
refreshViolationTable();
```

**الميزات:**
- تطبيق الفلاتر الحالية
- إعادة تشكيل الصفوف
- تحديث البيانات الحية

---

## فئات CSS

### نظام التبويبات

#### `.tab-navigation`
حاوية التبويبات
```css
.tab-navigation {
  display: flex;
  background: white;
  gap: var(--spacing-md);
}
```

#### `.tab-btn`
زر التبويب
```css
.tab-btn {
  padding: var(--spacing-sm) var(--spacing-lg);
  background-color: var(--light-gray);
  transition: all 0.3s ease;
}

.tab-btn.active {
  background-color: var(--primary-olive);
  color: white;
}
```

#### `.tab-content`
محتوى التبويب
```css
.tab-content {
  display: none;
}

.tab-content.active {
  display: block;
}
```

---

### بطاقات الملخص

#### `.summary-card`
بطاقة الإحصائية
```css
.summary-card {
  background: white;
  border-right: 4px solid var(--gold);
  padding: var(--spacing-lg);
  border-radius: var(--radius-lg);
}
```

#### `.summary-card.warning`
بطاقة تحذير (مخالفات غير مدفوعة)
```css
.summary-card.warning {
  border-right-color: var(--warning);
}
```

#### `.summary-card.success`
بطاقة نجاح (مخالفات مدفوعة)
```css
.summary-card.success {
  border-right-color: var(--success);
}
```

---

### عناصر المخالفات

#### `.violation-item`
عنصر المخالفة
```css
.violation-item {
  background: white;
  border-left: 4px solid var(--warning);
  padding: var(--spacing-lg);
}
```

#### `.violation-item.paid`
المخالفة المدفوعة
```css
.violation-item.paid {
  border-left-color: var(--success);
  opacity: 0.9;
}
```

---

### الجداول

#### `.violations-table`
جدول المخالفات
```css
.violations-table {
  width: 100%;
  border-collapse: collapse;
}
```

#### `.status-badge`
شارة الحالة
```css
.status-badge {
  display: inline-block;
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--radius-md);
}

.badge-success {
  background-color: rgba(16, 185, 129, 0.1);
  color: var(--success);
}

.badge-warning {
  background-color: rgba(245, 158, 11, 0.1);
  color: var(--warning);
}
```

---

### بطاقات الوثائق

#### `.document-card`
بطاقة الوثيقة
```css
.document-card {
  background: white;
  border-top: 3px solid var(--gold);
  padding: var(--spacing-lg);
  text-align: center;
}
```

#### `.documents-grid`
شبكة الوثائق
```css
.documents-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: var(--spacing-lg);
}
```

---

### قسم الدفع

#### `.payment-summary`
ملخص الدفع
```css
.payment-summary {
  background: linear-gradient(135deg, var(--primary-olive) 0%, var(--dark-olive) 100%);
  color: white;
  padding: var(--spacing-2xl);
}
```

#### `.payment-item`
عنصر الدفع
```css
.payment-item {
  background: white;
  border-right: 4px solid var(--warning);
  padding: var(--spacing-lg);
}
```

---

### النموذج

#### `.modal`
نموذج مشروط
```css
.modal {
  display: none;
  position: fixed;
  top: 0; right: 0; bottom: 0; left: 0;
  background-color: rgba(0, 0, 0, 0.5);
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.modal.active {
  display: flex;
}
```

---

## البيانات والحالات

### بنية المخالفة

```javascript
{
  id: 'VIO-2024-00156',        // معرّف فريد
  type: 'تجاوز السرعة',         // نوع المخالفة
  date: '2024-12-15',          // تاريخ المخالفة (YYYY-MM-DD)
  location: 'طريق الملك فهد',  // موقع المخالفة
  amount: 500,                 // المبلغ (ر.س)
  status: 'unpaid'             // الحالة (paid/unpaid)
}
```

### حالة التطبيق

```javascript
const trafficState = {
  violations: [...],           // مصفوفة المخالفات
  filters: {
    vehicleNumber: '',
    dateFrom: '',
    dateTo: '',
    violationType: '',
    status: ''
  },
  currentTab: 'myViolations',
  currentPaymentViolation: null
};
```

### البيانات المثال

التطبيق يحتوي على 7 مخالفات تجريبية:
- 3 مخالفات غير مدفوعة
- 4 مخالفات مدفوعة
- إجمالي المبلغ: 2,500 ر.س

---

## الاستجابة والطباعة

### نقاط التوقف (Breakpoints)

```css
/* الشاشات الكبيرة (Desktop) */
@media (min-width: 1200px)

/* الشاشات المتوسطة (Tablet) */
@media (max-width: 1200px) and (min-width: 768px)

/* الشاشات الصغيرة (Mobile) */
@media (max-width: 768px)

/* الشاشات الصغيرة جداً */
@media (max-width: 480px)
```

### تعديلات الاستجابة

#### على الشاشات الصغيرة:
- التبويبات تصبح عمودية
- الشبكات تتحول إلى عمود واحد
- الأزرار تصبح بحجم كامل العرض
- الجدول يصير أصغر حجماً

### نمط الطباعة

```css
@media print {
  header, aside, .print-hide {
    display: none;
  }
  
  body {
    background: white;
    margin: 0;
    padding: 20px;
  }
  
  .violation-item {
    page-break-inside: avoid;
  }
}
```

### قائمة العناصر المخفية عند الطباعة:
- Header (الرأس)
- Sidebar (الشريط الجانبي)
- أزرار الإجراءات
- النماذج والمدخلات

---

## أمثلة الاستخدام

### مثال 1: تبديل التبويب

```html
<!-- زر التبويب -->
<button class="tab-btn" onclick="switchTab('payment')">
  الدفع والمتابعة
</button>

<!-- محتوى التبويب -->
<div id="payment" class="tab-content">
  <!-- محتوى قسم الدفع -->
</div>
```

### مثال 2: عرض المخالفات

```javascript
// إضافة مخالفة جديدة
trafficState.violations.push({
  id: 'VIO-2024-00157',
  type: 'عدم ارتداء حزام الأمان',
  date: '2024-12-20',
  location: 'حي الملقا',
  amount: 400,
  status: 'unpaid'
});

// تحديث العرض
updateSummaryCards();
refreshViolationTable();
```

### مثال 3: معالجة الدفع

```html
<!-- زر الدفع -->
<button onclick="openPaymentModal('VIO-2024-00156', 500)">
  دفع الآن
</button>

<!-- نموذج الدفع -->
<form id="paymentForm" onsubmit="event.preventDefault(); simulatePayment();">
  <input type="text" id="cardNumber" placeholder="رقم البطاقة" />
  <input type="text" id="expiryDate" placeholder="MM/YY" />
  <input type="text" id="cvv" placeholder="CVV" />
  <button type="submit">تأكيد الدفع</button>
</form>
```

---

## الدعم والصيانة

### المشاكل الشائعة والحلول

**المشكلة:** التبويبات لا تتبدل
- **الحل:** تأكد من تحميل js/traffic.js
- **الحل:** تحقق من معرّفات التبويبات

**المشكلة:** البيانات لا تتحدث
- **الحل:** استدعِ `updateSummaryCards()` و `refreshViolationTable()`
- **الحل:** تحقق من بنية البيانات

**المشكلة:** النموذج لا يفتح
- **الحل:** تأكد من وجود عنصر `#paymentModal`
- **الحل:** تحقق من الحقول المطلوبة

### نصائح التطوير

1. **استخدم متفقد الكود** (Developer Tools) للتصحيح
2. **تحقق من وحدة التحكم** (Console) للأخطاء
3. **اختبر على أجهزة مختلفة** للاستجابة
4. **استخدم localStorage** لتخزين البيانات بين الجلسات

---

## ملاحظات أخيرة

هذه الوحدة مصممة لتكون:
- ✅ **احترافية**: تصميم حكومي معاصر
- ✅ **سهلة الاستخدام**: واجهة بديهية
- ✅ **مستجيبة**: تعمل على جميع الأجهزة
- ✅ **قابلة للتطوير**: يمكن إضافة مميزات جديدة

---

**آخر تحديث:** 2024-12-20  
**الإصدار:** 1.0  
**الحالة:** ✅ جاهز للاستخدام

